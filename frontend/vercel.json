{
  "version": 2,
  "builds": [
    {
      "src": "frontend/src/index.js",   // Make sure this points to the entry file of your React app
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
